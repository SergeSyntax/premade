import { describe, expect, it } from '@premade/ms-common/tests/utils';

describe('auth', () => {
  it('should be configured', async () => {
    // logger.info('test');
    expect(1).toBe(1);
  });
});
